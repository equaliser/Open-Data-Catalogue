<article id="licenceNew" class="item new">
	<header>
		<h1>New licence</h1>
	</header>

<% form_for([:admin, @licence]) do |f| %>
 
	<%= render "form", :f => f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_licences_path %>

</article>