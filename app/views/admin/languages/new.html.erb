<article id="languageNew" class="item new">
	<header>
		<h1>New language</h1>
	</header>

<% form_for([:admin, @language)] do |f| %>

  <%= render "form", :f => f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_languages_path %>

</article>