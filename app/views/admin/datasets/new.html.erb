<article id="datasetNew" class="item new">
	<header>
		<h1>New dataset</h1>
	</header>

<% form_for([:admin, @dataset]) do |f| %>

  <%= render "form", :f => f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_datasets_path %></article>